# docker-compose.yml

version: '3.8'

services:
  api:
    build: .  
    ports:
      - "8003:8000"  
    environment:
      - RABBITMQ_HOST=rabbitmq
    depends_on:
      - rabbitmq  

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"  
      - "15672:15672" 